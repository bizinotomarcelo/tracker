# Outros arquivos da pasta — não fazem parte do app
plano_treino_ppl_upper_marcelo.html
protocolo_nutricional_marcelo.html
tela de início.png
ícone.png

# Sessões de brainstorming (mockups temporários)
.superpowers/

# Chave de serviço Firebase (NUNCA commitar)
serviceAccountKey.json

# Plano gerado pelo Claude (temporário, publicado via publish-plan.js)
plan.json
